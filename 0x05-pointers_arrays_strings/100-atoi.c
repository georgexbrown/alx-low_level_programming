#include <stdlib.h>
#include "main.h"

/**
 * _atoi - Function that converts a string to integer
 * @s: Character pointer parameter
 *
 * Return: an integer type
 */
int _atoi(char *s)
{
	int c;

	c = atoi(s);
	return (c);
}
