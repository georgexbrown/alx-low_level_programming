#include <stdio.h>

/**
 * _puts_recursion - Function that prints a string followed by a new line
 * @s: character pointer parameter that hold the string
 */
void _puts_recursion(char *s)
{
	puts(s);
}
