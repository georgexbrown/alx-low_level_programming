#include "main.h"

/**
 * mul - Multiplying two integers
 * @a: integer parameter to multiply b and return value
 * @b: integer parameter to multiply a and return value
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
